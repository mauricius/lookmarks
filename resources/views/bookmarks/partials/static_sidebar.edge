@component('components.title')
  Categories
@endcomponent

@!component('components.button_link', href = route('categories.index'), label = 'Manage Categories')

<div class="py-3">
  <ul>
    @each(category in categories)
    <li class="hover:bg-gray-300 px-1 py-2">
      <a href="#{{ category.slug }}" class="text-gray-700 block">{{ category.name }} ({{ category.__meta__.bookmarks_count }})</a>
    </li>
    @endeach
  </ul>
</div>