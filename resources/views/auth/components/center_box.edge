<div class="mx-auto h-full flex justify-center items-center">
  <div class="w-1/3 mt-6">
    <h1 class="text-gray-700 mb-6 text-center text-4xl">{{ title }}</h1>

    <div class="border-teal-500 p-8 border-t-12 bg-white mb-6 text-gray-700 rounded-lg shadow-lg">
      @if($slot.header)
        {{{ $slot.header }}}
      @endif

      @!yield($slot.body)
    </div>

    @yield($slot.footer)

    @endyield
  </div>
</div>